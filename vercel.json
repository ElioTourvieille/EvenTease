{
  "projects": {
    "my-react-app": {
      "root": "./client",
      "commands": {
        "dev": "npm run start",
        "build": "npm run build"
      }
    },
    "my-node-api": {
      "root": "./server",
      "commands": {
        "dev": "node index.js",
        "build": "npm install && npm run build"
      }
    }
  }
}